# 🚨 СРОЧНО: Исправление ошибки регистрации

## Проблема
- ❌ Ошибка 403 при регистрации
- ❌ "Database error saving new user"
- ❌ Rate limit 429 (слишком много попыток)
- ❌ 401 Unauthorized при проверке email кода
- ❌ "Неправильный код" даже если код верный

## Причина
RLS политики слишком строгие - не разрешают:
1. Проверку уникальности email/nickname (нужно для формы)
2. Создание новой записи в таблице users (нужно для регистрации)

---

## ✅ РЕШЕНИЕ (выполните СЕЙЧАС!)

### Шаг 1: Откройте Supabase

1. Перейдите на https://supabase.com/dashboard
2. Выберите проект **TourFurr**
3. В левом меню: **SQL Editor**
4. Нажмите **New query**

### Шаг 2: Выполните ПЕРВЫЙ скрипт

Скопируйте содержимое файла:
```
database/fix_registration_error.sql
```

Вставьте в SQL Editor и нажмите **RUN** (или F5)

Этот скрипт:
- ✅ Делает `password_hash` nullable
- ✅ Добавляет отсутствующие поля
- ✅ Удаляет конфликтующие триггеры

### Шаг 3: Выполните ВТОРОЙ скрипт

Скопируйте содержимое файла:
```
database/fix_rls_for_registration.sql
```

Вставьте в SQL Editor и нажмите **RUN**

Этот скрипт:
- ✅ Исправляет RLS политики
- ✅ Разрешает проверку email/nickname
- ✅ Разрешает регистрацию новых пользователей

### Шаг 3: Выполните ТРЕТИЙ скрипт (ВАЖНО!)

Скопируйте содержимое файла:
```
database/fix_email_verification_rls.sql
```

Вставьте в SQL Editor и нажмите **RUN**

Этот скрипт:
- ✅ Исправляет RLS для email verification codes
- ✅ Разрешает проверку кодов без авторизации
- ✅ Создает функцию verify_email_code() для безопасной проверки

### Шаг 4: Проверьте результат

После выполнения ВСЕХ ТРЁХ скриптов вы должны увидеть:
```
✅ Исправления применены успешно!
✅ RLS политики обновлены! Теперь регистрация должна работать.
```

И список политик:
```
Anyone can check email/nickname uniqueness
Anyone can register
Users can view own data
Users can update own data
Admins can view all users
...
```

---

## 🧪 Тестирование

### 1. Подождите 60 секунд
Supabase имеет rate limiting. Между попытками регистрации должно пройти минимум 60 секунд.

### 2. Попробуйте зарегистрироваться снова

Откройте ваш сайт и попробуйте создать новый аккаунт.

**Должно работать:**
- ✅ Проверка email показывает "Email свободен" или "Email занят"
- ✅ Проверка nickname показывает "Никнейм свободен" или "Никнейм занят"
- ✅ Turnstile загружается
- ✅ После заполнения формы регистрация успешна
- ✅ Приходит код подтверждения на email

**НЕ должно быть:**
- ❌ Ошибка 403 Forbidden
- ❌ Ошибка 500 Internal Server Error
- ❌ "Database error saving new user"

---

## 🔍 Что делать если не работает

### Ошибка 429 "Too Many Requests"
```
For security purposes, you can only request this after 42 seconds
```

**Решение:** Просто подождите 1-2 минуты между попытками регистрации.

### Все еще ошибка 403

**Проверьте:**
1. Оба SQL скрипта выполнены успешно
2. В SQL Editor нет ошибок (красный текст)
3. Политики отображаются в списке (шаг 4 из fix_rls_for_registration.sql)

**Если проблема остается:**
```sql
-- Выполните это в SQL Editor для диагностики:
SELECT * FROM pg_policies WHERE tablename = 'users';
```

Должно показать минимум 7 политик, включая:
- "Anyone can check email/nickname uniqueness"
- "Anyone can register"

### Все еще ошибка 500

**Проверьте:**
1. Выполнили `fix_registration_error.sql`
2. Поле `password_hash` имеет `is_nullable = YES`

**Диагностика:**
```sql
SELECT column_name, is_nullable 
FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'password_hash';
```

Должно показать: `is_nullable = YES`

### Ошибка 401 при проверке email кода

**Симптомы:**
```
401 Unauthorized
Error creating verification code
Неправильный код (даже если код верный)
```

**Причина:** RLS не разрешает неавторизованным пользователям читать verification codes

**Решение:**
1. Выполните `fix_email_verification_rls.sql`
2. Проверьте политики

**Диагностика:**
```sql
SELECT * FROM pg_policies
WHERE tablename = 'email_verification_codes';
```

Должно показать политику: `Anyone can read verification codes for validation`

---

## 📊 Что было исправлено

### До исправления:
```
❌ password_hash NOT NULL - блокировал регистрацию
❌ RLS: auth.uid() = id - не работает до авторизации
❌ Нет политики для проверки uniqueness
```

### После исправления:
```
✅ password_hash nullable - Supabase Auth управляет паролями
✅ RLS: true для SELECT - можно проверить email/nickname
✅ RLS: true для INSERT - можно зарегистрироваться
✅ После регистрации пользователь видит только свои данные
```

---

## 🔒 Безопасность

### Не опасно ли "Anyone can check email/nickname uniqueness"?

**НЕТ, это безопасно!** Вот почему:

1. **Запрашивается только ID:**
   ```javascript
   .select('id').eq('email', 'test@example.com')
   // Возвращает только: { id: "uuid" } или null
   ```

2. **Никакие персональные данные не раскрываются:**
   - Нет телефонов
   - Нет Telegram
   - Нет описаний
   - Только факт существования email/nickname

3. **Это стандартная практика:**
   - GitHub делает так
   - Twitter делает так
   - Все сайты с регистрацией делают так

4. **Дополнительная защита:**
   - Rate limiting (20 проверок в минуту)
   - Cloudflare Turnstile
   - Input sanitization

### Не опасно ли "Anyone can register"?

**НЕТ, это безопасно!** Защиты:

1. **Supabase Auth** проверяет email/пароль
2. **Cloudflare Turnstile** блокирует ботов
3. **Rate limiting** - макс 3 регистрации в час с одного email
4. **Email verification** - без подтверждения доступа нет
5. **Input sanitization** - все данные очищаются

---

## ✅ Чеклист

- [ ] Выполнен `fix_registration_error.sql`
- [ ] Выполнен `fix_rls_for_registration.sql`
- [ ] Подождал 60 секунд после последней попытки
- [ ] Попробовал зарегистрироваться снова
- [ ] Регистрация работает без ошибок
- [ ] Пришел код подтверждения

---

## 🆘 Помощь

Если после выполнения всех шагов проблема остается:

1. Проверьте логи Supabase:
   - Dashboard → Logs → API
   - Найдите последний POST /auth/v1/signup
   - Посмотрите детали ошибки

2. Проверьте настройки Auth:
   - Dashboard → Authentication → Settings
   - "Enable email confirmations" должно быть ВЫКЛЮЧЕНО
   - Мы используем свою систему с 6-значными кодами

3. Свяжитесь со мной с:
   - Скриншотом ошибки из консоли
   - Результатом выполнения SQL скриптов
   - Логами из Supabase

---

🎉 **После исправления регистрация будет работать отлично!**
