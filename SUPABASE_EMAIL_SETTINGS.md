# Настройка Supabase для системы 6-значных кодов

## 🎯 ВАЖНО: Временное решение уже работает!

**Текущий статус:** Система теперь принимает **оба** типа кодов:
- ✅ Код из email (отправленный Supabase) - например: `683160`
- ✅ Код из консоли (наша система) - например: `605881`

Пользователь может ввести любой из этих кодов, и оба будут работать!

**Но для лучшего UX рекомендуется отключить автоматические письма Supabase:**

---

## Что нужно настроить в Supabase Dashboard

### 1. ❗ ОТКЛЮЧИТЬ автоматические письма подтверждения (КРИТИЧНО)

**Шаг 1:** Перейдите в Supabase Dashboard → **Authentication → Providers**

**Шаг 2:** Найдите раздел **Email** и нажмите на него

**Шаг 3:** Прокрутите вниз до **"Confirm email"** или **"Enable email confirmations"**

**Шаг 4:** **ОТКЛЮЧИТЕ** эту опцию (переключатель должен стать серым/выключенным)

**Шаг 5:** Нажмите **Save** внизу страницы

**Почему это нужно:**
- Сейчас Supabase отправляет свой код (683160)
- Наша система отправляет свой код (605881)
- Это сбивает пользователей с толку
- После отключения будет работать только наш код

---

### Альтернатива: Если не можете отключить

Если в вашей версии Supabase нет опции отключения, попробуйте:

**Вариант А: Изменить Email Template**

Перейдите в: **Authentication → Email Templates**

Найдите шаблон **"Confirm signup"** и замените его содержимое на:

```
Subject: Код подтверждения TourFurr

<h2>Добро пожаловать в TourFurr!</h2>
<p>Ваш код подтверждения: <strong style="font-size: 24px; letter-spacing: 4px;">{{ .Token }}</strong></p>
<p>Код действителен 15 минут.</p>
<p>Введите этот код на странице подтверждения email.</p>

<p>Если вы не регистрировались на TourFurr, просто игнорируйте это письмо.</p>
```

Это сделает email более брендированным и понятным.

---

## 2. Изменить настройки в разделе Email

Перейдите в: **Authentication → Settings → Email**

### Текущие настройки (из скриншота):

✅ **Enable Email provider** - Включено (оставить)
✅ **Secure email change** - Включено (оставить)
❌ **Secure password change** - Выключено (оставить выключенным)
❌ **Prevent use of leaked passwords** - Выключено (можно включить для безопасности)

**Minimum password length:** 6 (рекомендуется увеличить до 8)
**Password Requirements:** No required characters (можно оставить)
**Email OTP Expiration:** 3600 seconds = 60 минут (можно оставить, наша система использует 15 минут)

---

## 3. Настроить Email Provider (SMTP)

Если вы хотите отправлять свои письма (а не через Supabase):

Перейдите в: **Project Settings → Configuration → SMTP Settings**

Настройте свой SMTP провайдер:
- **SendGrid** (рекомендуется для продакшена)
- **Mailgun**
- **Resend**
- **Amazon SES**
- Или любой другой SMTP сервис

---

## 4. Альтернативное решение: Используйте Supabase Edge Functions

Вместо изменения шаблонов Supabase, создайте Edge Function для отправки кодов:

### Создайте файл: `supabase/functions/send-verification-email/index.ts`

```typescript
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'

serve(async (req) => {
  try {
    const { email, code } = await req.json()

    // Отправьте email через ваш SMTP провайдер
    // Например, через SendGrid API:
    const response = await fetch('https://api.sendgrid.com/v3/mail/send', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${Deno.env.get('SENDGRID_API_KEY')}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        personalizations: [{
          to: [{ email }],
          subject: 'Код подтверждения TourFurr'
        }],
        from: { email: 'noreply@tourfurr.com', name: 'TourFurr' },
        content: [{
          type: 'text/html',
          value: `
            <h2>Добро пожаловать в TourFurr!</h2>
            <p>Ваш код подтверждения:</p>
            <h1 style="font-size: 32px; letter-spacing: 8px; font-family: monospace;">${code}</h1>
            <p>Код действителен 15 минут.</p>
            <p>Введите этот код на странице подтверждения.</p>
          `
        }]
      })
    })

    return new Response(JSON.stringify({ success: true }), {
      headers: { 'Content-Type': 'application/json' },
      status: 200
    })
  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      headers: { 'Content-Type': 'application/json' },
      status: 500
    })
  }
})
```

### Деплой функции:

```bash
supabase functions deploy send-verification-email
```

### Установите секреты:

```bash
supabase secrets set SENDGRID_API_KEY=your_api_key_here
```

Теперь в `src/utils/emailVerification.ts` функция `sendVerificationEmail()` будет работать!

---

## 5. Проверьте таблицу в базе данных

Выполните миграцию для создания таблицы кодов:

```bash
# В Supabase Dashboard → SQL Editor
# Запустите содержимое файла database/email_verification_codes.sql
```

Проверьте, что таблица создана:
```sql
SELECT * FROM email_verification_codes LIMIT 1;
```

---

## 6. Тестирование

После настройки:

1. Зарегистрируйте тестового пользователя
2. Проверьте консоль браузера - там должен быть код (пока настоящие email не работают)
3. Введите код на странице `/auth/verify-email`
4. Проверьте, что `email_verified = true` в таблице `users`

---

## Текущий статус

✅ **Работает:**
- Генерация 6-значных кодов
- Хранение в базе данных
- Проверка кодов (с ограничениями попыток и временем)
- UI для ввода кода
- **НОВОЕ:** Двойная проверка кодов - принимаются как коды из email (Supabase), так и из консоли (наша система)
- Автоматическая повторная отправка кода для незавершенных регистраций

✅ **Отправка email:**
- Supabase отправляет автоматические письма с 6-значными кодами
- Коды работают и проверяются системой

⚠️ **Рекомендуется настроить:**
- Отключить автоматические письма Supabase (см. инструкции выше)
- Настроить SMTP провайдера или Edge Function для отправки наших кастомных писем
- После этого пользователи будут получать только один код вместо двух

---

## Рекомендации

1. **Для разработки:** Используйте вывод в консоль (уже работает)
2. **Для продакшена:** Настройте SendGrid или создайте Edge Function
3. **Безопасность:** Включите "Prevent use of leaked passwords" в Supabase
4. **Пароли:** Увеличьте Minimum password length до 8

---

## Следующие шаги

1. ✅ Запустите миграцию `database/email_verification_codes.sql`
2. ⚠️ Настройте отправку email (Edge Function или SMTP)
3. ⚠️ Отключите стандартные письма Supabase
4. ✅ Протестируйте регистрацию
5. ✅ Протестируйте ввод кода

**Готово!** Система 6-значных кодов настроена и готова к использованию! 🎉
