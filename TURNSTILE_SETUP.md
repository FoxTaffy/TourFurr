# 🔧 Исправление ошибки Cloudflare Turnstile

## Проблема
Ошибка: **"Неверный домен. Если ошибка повторится, обратитесь к администратору сайта."**

## Причина
Cloudflare Turnstile блокирует запросы с доменов, которые не добавлены в список разрешенных доменов в настройках виджета.

## ✅ Решение уже применено

Я создал файл `.env` с **тестовым ключом** Cloudflare:
```bash
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA
```

**Этот ключ:**
- ✅ Работает на ЛЮБОМ домене (localhost, fourfurr.camp, vercel.app и т.д.)
- ✅ Всегда проходит проверку (для разработки)
- ✅ Не требует настройки в Cloudflare Dashboard

## 🚀 Что дальше

### 1. Перезапустите dev server

Остановите текущий сервер (Ctrl+C) и запустите заново:

```bash
npm run dev
```

Vite подхватит новые переменные окружения из `.env` файла.

### 2. Обновите страницу

Откройте приложение в браузере и нажмите **Ctrl+Shift+R** (полная перезагрузка).

### 3. Проверьте форму регистрации

Теперь Cloudflare Turnstile должен работать без ошибки "Неверный домен".

---

## 🔐 Для продакшена (когда будете деплоить на fourfurr.camp)

### Шаг 1: Создайте виджет Turnstile

1. Откройте [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в раздел **Turnstile**
3. Нажмите **Add Site**
4. Заполните форму:
   - **Site name:** TourFurr Production
   - **Domain:** `fourfurr.camp`
   - **Widget Mode:** Managed
5. Добавьте дополнительные домены:
   - `www.fourfurr.camp`
   - `localhost` (для разработки)
   - Ваш Vercel preview домен (если используете)

### Шаг 2: Получите ключи

После создания виджета скопируйте:
- **Site Key** (начинается с `0x...`)
- **Secret Key** (начинается с `0x...`)

### Шаг 3: Обновите конфигурацию

**В Vercel Dashboard:**

1. Откройте **Settings** → **Environment Variables**
2. Добавьте:
   ```
   VITE_TURNSTILE_SITE_KEY = ваш_реальный_site_key
   ```

**В Supabase Dashboard:**

1. Откройте **Project Settings** → **Edge Functions** → **Secrets**
2. Добавьте секрет:
   - **Name:** `TURNSTILE_SECRET_KEY`
   - **Value:** ваш_secret_key

### Шаг 4: Redeploy

После обновления переменных окружения сделайте redeploy:

```bash
git push origin main
```

Vercel автоматически задеплоит с новыми ключами.

---

## 📋 Альтернативные тестовые ключи

Cloudflare предоставляет несколько тестовых ключей:

| Ключ | Поведение | Когда использовать |
|------|-----------|-------------------|
| `1x00000000000000000000AA` | ✅ Всегда успешен | **Текущий** - для локальной разработки |
| `2x00000000000000000000AB` | ❌ Всегда ошибка | Для тестирования обработки ошибок |
| `3x00000000000000000000FF` | 🔄 Интерактивный | Для тестирования UX |

## ❓ FAQ

**Q: Нужно ли менять что-то в коде?**
A: Нет, код уже правильно настроен. Нужно только перезапустить dev server.

**Q: Безопасно ли использовать тестовый ключ?**
A: Да, для локальной разработки это абсолютно безопасно. Для продакшена используйте реальный ключ.

**Q: Как проверить, что Turnstile работает?**
A: Откройте форму регистрации. Если виджет Turnstile появляется без ошибок - всё работает.

**Q: Что если ошибка всё ещё появляется?**
A: Проверьте:
1. Перезапустили ли вы dev server (`npm run dev`)
2. Есть ли файл `.env` в корне проекта
3. Полностью перезагрузили ли страницу (Ctrl+Shift+R)

---

**Создано:** 2026-01-31
**Проблема решена:** ✅
