# 🧪 Инструкция по тестированию TourFurr (Режим разработки)

## 📋 Оглавление
1. [Настройка окружения](#настройка-окружения)
2. [Тестирование Email-подтверждения](#тестирование-email-подтверждения)
3. [Проверка безопасности](#проверка-безопасности)
4. [Типичные сценарии](#типичные-сценарии)

---

## 🔧 Настройка окружения

### Шаг 1: Создайте файл `.env`

```bash
cp .env.example .env
```

### Шаг 2: Включите режим разработки

В файле `.env` установите:

```env
VITE_DISABLE_EMAIL=true
```

**Важно!** В этом режиме:
- ✅ Email не отправляются реально
- ✅ Коды выводятся в консоль браузера (F12)
- ✅ Отсутствуют лимиты Resend API
- ⚠️ Используется только для локальной разработки

### Шаг 3: Заполните остальные переменные

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
VITE_ADMIN_PIN=123456  # Измените на безопасный PIN
```

### Шаг 4: Запустите приложение

```bash
npm run dev
```

---

## 📧 Тестирование Email-подтверждения

### Регистрация нового пользователя

1. **Откройте консоль браузера** (F12 → Console)
2. **Перейдите на страницу регистрации** (`/auth?tab=register`)
3. **Заполните форму регистрации**:
   - Email: `test@example.com`
   - Пароль: `Test123!@#` (должен пройти проверку сложности)
   - Никнейм, телефон, Telegram и т.д.
4. **Отправьте форму**
5. **В консоли появится код**:

```
================================================================================
🚀 РЕЖИМ РАЗРАБОТКИ: Email отправка отключена
================================================================================
📧 Email: test@example.com
🔑 КОД ПОДТВЕРЖДЕНИЯ: 123456
⏰ Действителен: 15 минут
💡 Инструкция: Скопируйте код выше и вставьте на странице подтверждения
================================================================================
```

6. **Скопируйте код** (например, `123456`)
7. **Введите код на странице подтверждения**
8. **Успех!** Email подтверждён

### Тестирование повторной отправки кода

1. На странице подтверждения нажмите **"Отправить код повторно"**
2. В консоли появится новый код
3. Старый код станет недействительным

---

## 🔒 Проверка безопасности

### 1. Rate Limiting (Защита от брутфорса)

#### Тест: Слишком много попыток входа

```
Лимит: 5 попыток за 15 минут
Блокировка: 30 минут
```

**Как проверить:**
1. Попробуйте войти с неверным паролем 6 раз подряд
2. На 6-й попытке должно появиться сообщение:
   > "Слишком много попыток входа. Попробуйте через X минут"

#### Тест: Слишком много регистраций

```
Лимит: 3 попытки за час
Блокировка: 2 часа
```

**Как проверить:**
1. Попробуйте зарегистрировать 4 разных аккаунта подряд
2. На 4-й попытке должно появиться сообщение:
   > "Слишком много попыток регистрации. Попробуйте через X минут"

### 2. Input Sanitization (Защита от XSS)

**Попробуйте ввести вредоносные данные:**

```html
<!-- В поле никнейма -->
<script>alert('XSS')</script>

<!-- В описании -->
<img src=x onerror="alert('XSS')">

<!-- В Telegram username -->
javascript:alert('XSS')
```

**Ожидаемый результат:**
- Опасные символы удаляются автоматически
- Или появляется сообщение: "Обнаружена подозрительная активность"

### 3. SQL Injection Protection

**Попробуйте ввести SQL-инъекции:**

```sql
-- В поле email
test@example.com'; DROP TABLE users; --

-- В поле никнейма
admin' OR '1'='1
```

**Ожидаемый результат:**
- Обнаружена подозрительная активность
- Регистрация/вход заблокированы

### 4. Password Strength (Сложность пароля)

**Слабые пароли (должны быть отклонены):**
- `123456` - слишком простой
- `password` - очевидный пароль
- `qwerty` - клавиатурный шаблон
- `Test123` - нет специальных символов

**Сильные пароли (должны быть приняты):**
- `MyP@ssw0rd123!` ✅
- `Secure#Pass2024` ✅
- `C0mpl3x!Pwd$` ✅

**Требования:**
- ✅ Минимум 8 символов
- ✅ Заглавные и строчные буквы
- ✅ Цифры
- ✅ Специальные символы

### 5. Email Validation

**Невалидные email (должны быть отклонены):**
- `test` - нет домена
- `test@` - нет домена
- `@example.com` - нет локальной части
- `test..test@example.com` - двойные точки

**Валидные email:**
- `test@example.com` ✅
- `user.name+tag@example.co.uk` ✅

### 6. Grace Period (Удаление неподтверждённых аккаунтов)

```
Лимит: 15 минут после регистрации
```

**Как проверить:**
1. Зарегистрируйтесь, но НЕ подтверждайте email
2. Подождите 15 минут
3. Аккаунт должен быть автоматически удалён
4. При попытке входа должно появиться:
   > "Email не подтверждён. Новый код отправлен на вашу почту."

---

## 🎯 Типичные сценарии

### Сценарий 1: Успешная регистрация

1. ✅ Открыть консоль (F12)
2. ✅ Зарегистрироваться с валидными данными
3. ✅ Скопировать код из консоли
4. ✅ Ввести код на странице подтверждения
5. ✅ Войти в систему

### Сценарий 2: Потерян код подтверждения

1. ✅ Зарегистрироваться
2. ❌ Закрыть вкладку (код потерян)
3. ✅ Попытаться войти
4. ✅ Получить новый код в консоли
5. ✅ Подтвердить email с новым кодом

### Сценарий 3: Истёк срок действия кода

1. ✅ Зарегистрироваться
2. ⏰ Подождать 15+ минут
3. ✅ Попытаться ввести старый код
4. ❌ Код недействителен
5. ✅ Запросить новый код

### Сценарий 4: Миграция старого пользователя

1. ✅ Войти со старым аккаунтом (с bcrypt паролем)
2. ✅ Система автоматически мигрирует на Supabase Auth
3. ✅ Пользователь остаётся авторизованным
4. ✅ При следующем входе используется Supabase Auth

---

## 🐛 Отладка

### Проверка логов безопасности

Откройте консоль и выполните:

```javascript
// Проверить события безопасности
window.__security_events = []

// Все события
console.log(securityLogger.getEvents())

// События для конкретного пользователя
console.log(securityLogger.getEvents('test@example.com'))

// Только неудачные попытки входа
console.log(securityLogger.getEvents(undefined, 'login_failure'))
```

### Проверка rate limiting

```javascript
// Проверить статус блокировки
rateLimiter.getBlockedTime('test@example.com')
// Вернёт: количество секунд до разблокировки

// Сбросить лимиты (для тестирования)
rateLimiter.reset('test@example.com')
```

### Проверка режима разработки

```javascript
// В консоли браузера
console.log(import.meta.env.VITE_DISABLE_EMAIL)
// Должно вернуть: "true"
```

---

## 🚀 Переход в продакшен

Когда купите домен и настроите email:

1. **Обновите .env:**
   ```env
   VITE_DISABLE_EMAIL=false
   ```

2. **Настройте Resend API:**
   - Получите API ключ на https://resend.com
   - Добавьте в Supabase Edge Function:
     ```bash
     supabase secrets set RESEND_API_KEY=re_your_key_here
     ```

3. **Настройте домен:**
   - Добавьте DNS записи для Resend
   - Верифицируйте домен

4. **Протестируйте:**
   - Зарегистрируйтесь с реальным email
   - Проверьте получение письма
   - Подтвердите код из письма

---

## ❓ FAQ

**Q: Код не появляется в консоли?**
A: Проверьте что `VITE_DISABLE_EMAIL=true` в `.env` файле

**Q: "Слишком много попыток" но я только начал тестировать?**
A: Очистите localStorage или используйте режим инкогнито

**Q: Как сбросить все данные для тестирования?**
A:
```javascript
// В консоли браузера
localStorage.clear()
sessionStorage.clear()
location.reload()
```

**Q: Коды одинаковые при каждой регистрации?**
A: Нет, коды генерируются случайно (6 цифр от 100000 до 999999)

---

## 📞 Контакты

Если нашли баг или уязвимость безопасности, сообщите в Issues.

**Безопасность критична!** 🔒
